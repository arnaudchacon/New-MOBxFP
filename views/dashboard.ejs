<!DOCTYPE html>
<html>
<head>
    <title>MOBILIS Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        /* Body Styles */
        body {
            font-family: Montserrat, sans-serif;
            background-image: linear-gradient(45deg, transparent 20%, #24368c 24%, transparent 25%),
                              linear-gradient(-45deg, transparent 20%, #24368c 24%, transparent 25%),
                              linear-gradient(-45deg, transparent 75%, #24368c 79%, transparent 80%);
            background-size: 30px 30px, 30px 30px;
            margin: 0;
            padding: 0;
            color: #24368c;
        }

        /* Main Title Styles */
        h1 {
            position: absolute;
            right: 10%;
            top: 15%;
            font-size: 2.5em;
            font-weight: 700;
        }

        /* Steps */
        .step {
            position: absolute;
            right: 10%;
            background-color: #f6f6f6;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            width: 200px;
            transition: transform 0.3s;
        }

        .step1 {
            top: 30%;
        }

        .step2 {
            top: 40%;
        }

        .step3 {
            top: 50%;
        }

        /* AI Explanation */
        .ai-explanation {
            position: absolute;
            bottom: 10%;
            left: 30%;
            background-color: #f6f6f6;
            padding: 30px;
            border: 2px solid #000;
            font-size: 1em;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1);
            width: 300px;
            height: 200px;
            clip-path: polygon(0% 0%, 80% 0%, 100% 25%, 100% 100%, 0% 100%);
        }

        /* Form Styles */
        form {
            background-color: #fff;
            padding: 40px 60px;
            border-radius: 8px;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            margin: 80px auto;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
        }

        input[type="text"], textarea {
            width: 100%;
            padding: 12px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            background-color: #333;
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.2s ease;
        }

        button:hover {
            background-color: #555;
        }

    </style>
</head>
<body>
    <h1>UTILISEZ NOTRE INTELLIGENCE ARTIFICIELLE</h1>

    <!-- Steps -->
    <div class="step step1">Étape 1: Entrez le nom et la description de votre projet.</div>
    <div class="step step2">Étape 2: Cliquez sur 'Submit'.</div>
    <div class="step step3">Étape 3: Préparez-vous à utiliser notre IA!</div>

    <!-- AI Explanation -->
    <div class="ai-explanation">
        Une fois redirigé vers l'éditeur intégré, notre IA vous aidera à créer le design parfait pour votre espace. Profitez de la technologie avancée pour obtenir des résultats exceptionnels.
    </div>
    
    <!-- Centered Form -->
    <form id="projectForm">
        <label for="projectName">Project Name:</label>
        <input type="text" id="projectName" name="name" required>

        <label for="projectDescription">Description:</label>
        <textarea id="projectDescription" name="description" rows="4" required></textarea>

        <button type="submit">Submit</button>
    </form>

    <script>
        document.getElementById("projectForm").addEventListener("submit", function(event) {
            console.log("Form submitted!");
            event.preventDefault();  // Prevent default form submission

            // Collect form data
            const formData = {
                name: document.getElementById("projectName").value,
                description: document.getElementById("projectDescription").value
            };

            fetch('/create-floorplanner-project', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(formData)
            })
            .then(response => {
                if (!response.ok) {
                    // This will capture server errors and response is not a valid JSON
                    return response.text().then(text => {
                        throw new Error(text);
                    });
                }
                return response.json();
            })
            .then(data => {
                if (data.editorUrl) {
                    // Redirect the user to the Floorplanner editor with the provided URL
                    window.location.href = data.editorUrl;
                } else if (data.message) {
                    alert('Error starting new project: ' + data.message);
                }
            })
            .catch(error => {
                // This will now capture any error messages, be it from the server or any other issue
                alert('Error starting new project: ' + error.message);
            });            
        });
    </script>
</body>
</html>
