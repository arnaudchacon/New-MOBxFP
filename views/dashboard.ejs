<!DOCTYPE html>
<html>
<head>
    <title>MOBILIS Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        /* Body Styles */
        body {
            font-family: Montserrat, sans-serif;
            background-image: linear-gradient(45deg, transparent 20%, #24368c 25%, transparent 25%),
                              linear-gradient(-45deg, transparent 20%, #24368c 25%, transparent 25%),
                              linear-gradient(-45deg, transparent 75%, #24368c 80%, transparent 80%);
            background-size: 30px 30px, 30px 30px;
            margin: 0;
            padding: 0;
            color: #24368c;
        }

        /* Main Title Styles */
        h1 {
            position: absolute;
            left: 2%;
            bottom: 2%;
            font-size: 4em;
            font-weight: 700;
            background-color: #24368c;
            color: white;
            padding: 10px 40px;
            border-radius: 10px;
        }

        /* Mobilis Header */
        .mobilis {
            position: absolute;
            right: 2%;
            top: 2%;
            font-size: 3em;
            font-weight: 700;
        }

        /* Steps */
        .step {
            position: absolute;
            right: 23%;  
            background-color: #fff;
            padding: 25px;
            border-radius: 5px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            width: 250px;
            font-size: 1.4em;
            transition: transform 0.3s;
        }

        .step1 {
            top: 24%;  /* Adjusted positioning */
        }

        .step2 {
            top: 39%;  /* Adjusted positioning */
        }

        .step3 {
            top: 52%;  /* Adjusted the distance */
        }

        /* Bold the etape numbers */
        .bold {
            font-weight: bold;
        }

        /* Hover effect for steps */
        .step:hover {
            transform: scale(1.05);
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.2);
        }

        /* Form Styles */
        form {
            background-color: #fff;
            padding: 40px 60px;
            border-radius: 8px;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            margin: 240px 11%;  /* Lowered more */
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
        }

        input[type="text"], textarea {
            width: 100%;
            padding: 12px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            background-color: #e3720f;
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.2s ease;
        }

        button:hover {
            background-color: #555;
        }

    </style>
</head>
<body>
    <h1>UTILISEZ NOTRE INTELLIGENCE ARTIFICIELLE</h1>
    <div class="mobilis">MOBILIS</div>

    <!-- Steps -->
    <div class="step step1"><span class="bold">Étape 1:</span> Entrez le nom et la description de votre projet.</div>
    <div class="step step2"><span class="bold">Étape 2:</span> Cliquez sur 'Submit'.</div>
    <div class="step step3"><span class="bold">Étape 3:</span> Préparez-vous à utiliser notre IA!</div>
    
<!-- Centered Form -->
<form id="projectForm">
    <label for="userEmail">Entrez votre adresse e-mail:</label>
    <input type="email" id="userEmail" name="email" required style="width: 80%;">

    <label for="projectName">Nom du projet:</label>
    <input type="text" id="projectName" name="name" required>

    <label for="projectDescription">Description:</label>
    <textarea id="projectDescription" name="description" rows="4" required></textarea>

    <button type="button" id="goToEditor">Go to Editor</button>
    <button type="button" id="goToRoomplanner">Go to Roomplanner</button>
</form>

    <script>
        document.getElementById("goToEditor").addEventListener("click", function() {
            // For simplicity, we're just redirecting to the editor for now:
            window.location.href = '/go-to-floorplanner-editor';
        });

        document.getElementById("goToRoomplanner").addEventListener("click", function() {
            // For simplicity, we're just redirecting to the roomplanner for now:
            window.location.href = '/go-to-roomplanner'; 
        });
    </script>
</body>
</html>
