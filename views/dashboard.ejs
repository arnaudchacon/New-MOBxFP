<!DOCTYPE html>
<html>
<head>
    <title>MOBILIS Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        /* Body Styles */
        @keyframes movingShadow {
            0%, 100% {
                background-position: 0% 0%;
            }
            50% {
                background-position: 100% 100%;
            }
        }

        body {
            font-family: Montserrat, sans-serif;
            background: linear-gradient(45deg, rgba(24,42,120,0.3), rgba(255,255,255,0.3)); 
            background-size: 200% 200%;
            animation: movingShadow 30s infinite;
            margin: 0;
            padding: 0;
            color: #24368c;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        /* Form Styles */
        form {
            background-color: #fff;
            padding: 40px 60px;
            border-radius: 8px;
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
        }

        input[type="text"], input[type="email"] {
            width: 100%;
            padding: 12px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        /* Button Styles */
        button {
            background-color: #24368c;
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.2s ease;
            margin-right: 10px;
        }

        button:last-child {
            margin-right: 0;
        }

        button:hover {
            background-color: #1a2759;
        }

        /* Explanatory Texts */
        .explanatory-texts {
            margin-top: 20px;
            max-width: 600px;
            text-align: center;
        }

        /* Modal Styles */
        #roomplannerModal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.8);
            z-index: 1000;
        }

        #roomplannerModal iframe {
            width: 90%;
            height: 80%;
            frameborder: 0;
            scrolling: yes;
            position: absolute;
            top: 10%;
            left: 5%;
        }

    </style>
</head>
<body>
    <!-- Centered Form -->
    <form id="projectForm">
        <label for="userEmail">Entrez votre adresse e-mail:</label>
        <input type="email" id="userEmail" name="email" required style="width: 100%;">

        <button type="button" id="goToEditor">Editor</button>
        <button type="button" id="goToRoomplanner">Roomplanner</button>
    </form>

    <div class="explanatory-texts">
        <p><strong>Roomplanner:</strong> Créez rapidement des plans d'étage interactifs.</p>
        <p><strong>Editor:</strong> Une expérience de conception d'intérieur plus détaillée.</p>
    </div>

    <!-- Modal HTML -->
    <div id="roomplannerModal">
        <iframe src="https://moltow.floorplanner.com"></iframe>
    </div>

    <script>
        document.getElementById("goToEditor").addEventListener("click", function() {
            window.location.href = '/go-to-floorplanner-editor';
        });

        document.getElementById("goToRoomplanner").addEventListener("click", function() {
            document.getElementById("roomplannerModal").style.display = "block";
        });

        document.getElementById("roomplannerModal").addEventListener("click", function(event) {
            if (event.target === event.currentTarget) {
                document.getElementById("roomplannerModal").style.display = "none";
            }
        });
    </script>
</body>
</html>